#!/usr/bin/env groovy

node ('master') {

    environment {
        DEV = 'Development'
        PROD = 'Production'
    }
        stage('Preparation') {
                    echo "Environment " + ENV
                echo ENV1
                echo ENV2
            }
}

